# قواعد العمل في هذا المستودع

هذا المستودع تعمل عليه **عدة جلسات Claude في الوقت نفسه على شجرة عمل واحدة** (نفس المجلد، نفس فهرس git، نفس قاعدة بيانات التطوير `movie-postgres:5433`). لا يوجد عزل تلقائي بين الجلسات؛ العزل يتحقق بالالتزام بما يلي.

## 1. لوحة التنسيق: `.claude/SESSIONS.md`

- ملف حيّ خارج git (مُتجاهَل في `.gitignore`). فيه: كل جلسة نشطة، نطاقها، الملفات التي تملكها، المنافذ التي تشغّلها، ما هو قيد التنفيذ، وطلبات موجهة لجلسات أخرى.
- **في أول دور من كل جلسة**: اقرأ اللوحة، وسجّل نفسك فيها (العنوان، النطاق، الملفات المتوقعة).
- **قبل أي تعديل على ملف**: تأكد من اللوحة أنه ليس مملوكاً لجلسة أخرى. إن كان مملوكاً، اكتب طلباً في قسم «طلبات» ولا تعدّله بنفسك.
- **قبل الالتزام (commit)**: حدّث سطر جلستك في اللوحة (آخر commit، ما بقي قيد التنفيذ).
- **عند إغلاق النطاق**: احذف سطر جلستك من اللوحة وأطلق ملفاتها.
- الجلسة المسماة «المنسّق بين الجلسات» تراجع اللوحة وتحلّ التعارضات؛ راسلها عند أي تعارض لا تستطيع حلّه وحدك.
- **اللوحة ملف واحد تعدّله عدة جلسات في اللحظة نفسها**: عدّل قسم جلستك فقط، وأضف صفوفاً إلى الجداول دون إعادة ترقيم أو تحرير صفوف غيرك. معرّفات الطلبات تبدأ بحرف المرسل (`A1`، `B1`، `C1`، `M1` للمنسّق). الرأس وجدول التعارضات وسجل الالتزامات يحرّرها المنسّق؛ أضف إليها صفاً إن لزم ولا تعدّل الموجود.

## 2. قواعد git (غير قابلة للتفاوض)

1. **الإضافة بالمسار الصريح فقط**: `git add <path> <path>`. ممنوع `git add -A` و`git add .` و`git commit -a`؛ الشجرة تحوي عمل جلسات أخرى غير المكتمل.
2. **قبل كل commit**: `git status --short` وتأكد أن المُدرَج (staged) هو ملفاتك وحدها.
3. **ممنوع بلا استثناء**: `git stash` · `git checkout -- <file>` · `git restore` · `git reset --hard` · `git clean` · `git rebase -i`. أي واحدة منها تمحو عمل جلسة أخرى. إن احتجت التراجع عن تعديلك أنت، أعد الملف يدوياً بالتحرير.
4. **الدفع (push)**: `git fetch origin` ثم `git push origin main` إن كان `origin/main` سلفاً لرأسك (fast-forward). إن لم يكن، لا تعمل `pull --rebase` ولا `merge` على شجرة فيها تعديلات جلسات أخرى غير ملتزمة؛ اكتب في اللوحة وليتولّ المنسّق الدمج.
5. الملف المشترك بين جلستين لا يُلتزم إلا من الجلسة المسجّلة مالكةً له في اللوحة، بعد أن تجمع تعديلات الأخرى.

## 3. البنية التحتية المشتركة

- **المنافذ**: كل جلسة تسجّل منافذها في اللوحة قبل تشغيل أي خادم. لا تُوقف خادم جلسة أخرى (`preview_stop` على serverId ليس لك، أو قتل عملية على منفذ لم تسجّله).
- **قاعدة بيانات التطوير** (`movie-postgres`): أي migration جديدة أو تعديل صفوف بالجملة أو `db-reset` يُعلَن في اللوحة **قبل** التنفيذ، لأن الجلسات الأخرى تختبر عليها حيّاً. الاختبارات الآلية تستخدم `postgres-test` وحدها.
- **`.claude/launch.json`**: أضف إعداداً باسم منفذك؛ لا تعدّل إعدادات الآخرين.
- **حساب التجارب**: إن أنشأت حساباً أو بيانات تجريبية، سجّل بريده في اللوحة حتى تعيد الجلسات الأخرى استخدامه بدل إنشاء غيره.
- **عقد الإقلاع (حادثة `f23ec48`، 2026-09-05)**: أي تغيير في أسماء متغيّرات البيئة، أو القيم المقبولة عند الإقلاع، أو هجرة جديدة، يُعلَن في اللوحة ويُضبط في Railway **قبل** الدفع؛ وإلا يبقى الاسم/القيمة القديمة مقبولة كمرادف لإصدار واحد مع تحذير في السجل، لا رفض إقلاع.
- **سكربتات البيانات اليدوية لمرة واحدة (حادثة انقطاع النشر، 2026-09-06)**: الهجرات (migrations) تُطبَّق تلقائيًا عند كل نشر ولا تحتاج قاعدة إضافية. أما أي **سكربت بيانات يدوي عمدًا** (لا يُشغَّل تلقائيًا بتصميم، مثل نشر عناوين الكتالوج) فتنفيذه على قاعدة التطوير **لا يعني** تنفيذه على الإنتاج. أي ميزة تتضمن سكربتًا كهذا لا تُعتبر منتهية فعليًا، ولا يُغلَق بندها في اللوحة، حتى يُؤكَّد تنفيذه على قاعدة الإنتاج تحديدًا (لا التطوير فقط) بعد كل نشر يحمله.

## 4. الملفات الساخنة (مشتركة بطبيعتها)

هذه الملفات يعدّلها أكثر من نطاق. لا تلمسها إلا بعد إعلان في اللوحة، وأبقِ التعديل أصغر ما يمكن:

`apps/frontend/app/page.tsx` · `apps/frontend/app/layout.tsx` · `.claude/launch.json` · `Makefile` · `package.json` · `apps/backend/package.json` · `.env.example` · `docs/README.md` · `docs/IMPLEMENTATION_STATUS.md` · `docs/ARCHITECTURE_DECISIONS.md` · `packages/shared/src/*`

## 5. التوثيق

- قرارات المنتج والتصميم تُسجَّل في `docs/` كما هو معمول به (ADR في `ARCHITECTURE_DECISIONS.md`، الحالة في `IMPLEMENTATION_STATUS.md`).
- كل تدقيق أو خطة يُثبَّت على commit محدد بالاسم في رأس الملف.

## 6. دورة الإنجاز (قاعدة دائمة من المالك، 2026-09-03)

عند كل إنجاز في نطاقك، وبلا انتظار إذن لكل خطوة:

1. اكتب للمالك ملخصاً لما أُنجز.
2. التزم تعديلاتك بالمسار الصريح وادفعها برسالة commit وصفية (القسم 2: `fetch` ثم `push` إن كان fast-forward، وإلا سطر في اللوحة للمنسّق).
3. اكتب للمالك مقترح الخطوة التالية.

## 7. اقتصاد التوكينز (قاعدة دائمة من المالك، 2026-09-04) — المنسّق يكلّف، والجلسات تنفّذ

المنسّق («تنسيق الجلسات وتحسين استهلاك التوكينز») هو من يحدّد المهمة التالية لكل جلسة في اللوحة §4. الجلسة لا تختار نطاقاً جديداً بنفسها؛ عند إنهاء مهمتها تحدّث سطرها في اللوحة وتراسل المنسّق بسطر واحد، ثم تبدأ التكليف التالي المكتوب لها.

1. **لا تقرأ وثيقة كاملة**. ابدأ بـ`grep -n '^#'` ثم اقرأ القسم المطلوب فقط. الممنوع قراءته كاملاً: `IMPLEMENTATION_STATUS.md` · `ARCHITECTURE_DECISIONS.md` · `SCHEMA.md` · `movie_taste_platform_blueprint_ar.md` · `DEMO_DATA_PLAN_*.md` · أرشيف اللوحة.
2. **اللوحة مقتضبة**: قسم الجلسة ≤ 12 سطراً، سطر «الآن» و«آخر commit» فقط يتغيّران. لا سرد ولا أرقام اختبارات ولا تاريخ في اللوحة؛ مكانها رسالة الـcommit (≤ 8 أسطر) لا الوثائق.
3. **الوثائق تُحدَّث بصف، لا بفقرة**: صف جدول في `IMPLEMENTATION_STATUS.md`، وADR ≤ 15 سطراً. لا تُضاف فقرات «تحقّق» سردية في الوثائق.
4. **الرسائل بين الجلسات**: ≤ 5 أسطر، مهمة واحدة، مرجع واحد (رقم التكليف في اللوحة أو مسار ملف). لا تكرار لما في اللوحة.
5. **الأوامر**: لا تشغّل مجموعة الاختبارات كاملة إلا قبل الـcommit مباشرة؛ أثناء العمل شغّل الملف المعني وحده. اطبع الملخص لا السجل الكامل (`| tail`).
6. **لا تحقق مزدوج**: ما التزمته جلسة أخرى واختبرته لا يُعاد التحقق منه إلا إن كسر شيئاً عندك.
7. **جلسة واحدة لكل نطاق** كما في جدول اللوحة «الجلسات والموديل». لا تُفتح جلسة جديدة إلا بقرار المالك بعد اقتراح المنسّق.
8. **الإجابة للمالك**: ملخص ≤ 10 أسطر بالعربية عند كل إنجاز، بلا إعادة شرح ما في الوثائق.

## 8. الموديل لكل نطاق (قاعدة دائمة من المالك، 2026-09-05)

المنسّق يوصي بالموديل في جدول اللوحة «الجلسات والموديل»، والمالك يبدّله من قائمة الجلسة في التطبيق:

1. **Opus** للنطاق الذي فيه هجرة أو ADR أو تغيير عقد API أو تشفير/أمان أو تصميم نموذج.
2. **Sonnet** لتشغيل السكربتات والتزام نتائجها، ولوحات النشر والجولات الحيّة، والتوثيق والتنسيق.
3. **Fable** لدور واحد محدّد (تدقيق شامل أو إعداد) ثم يعود النطاق إلى Opus/Sonnet.
4. **الجهد**: منخفض للتشغيل والتنسيق، متوسط للتطوير؛ عالٍ فقط لتدقيق أو قرار معماري واحد.
5. **السياق الطويل يكلّف كل دور**: بعد نحو 600 رسالة أنهِ المهمة الجارية، حدّث سطرك في اللوحة، وأغلق؛ التكليف التالي يبدأ في جلسة جديدة.
6. لا مراقب أو حلقة أسرع من 30 ثانية، ولا لقطة شاشة حيث يكفي `read_page` أو `curl`.
