# Demo catalog — enrichment report

Generated by `services/workers/src/enrich_catalog.py` on 2026-09-03 (mode: anthropic; 439 s).
Fixture: `catalog.demo.json` · entries: 300 · with fingerprint: 300 · extracted this run: 300 · skipped (already current): 0 · failed: 0.

## Provenance

| extractorVersion | Titles |
|---|---|
| enrichment-worker-v2 | 295 |
| enrichment-worker-v2+partial | 5 |

| modelVersion (as served) | Titles |
|---|---|
| claude-sonnet-5 | 300 |

Mean per-dimension confidence reported by the extractor: 0.60

## Failures (0)

None.

Refusals among them: 0 — each is a human-review item (BP §15.4), never re-tried blindly.

## Rules

- Evidence sent: the fixture's own Wikipedia lead and plot text plus year/genres/country/language; never user data.
- One extraction per (title, extractorVersion); `--force` is the only way to overwrite.
- `licenseStatus` stays `unknown` and `reviewStatus` `unreviewed`: a development fixture, not a rights registry.
