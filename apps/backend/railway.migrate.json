{
  "$schema": "https://railway.app/railway.schema.json",
  "//": "Board C-18 (ADR-88), release step (ADR-90). A SEPARATE Railway service (not the backend service above), same repo/root directory/Dockerfile, start command overridden to `npm run release`: pending migrations, then the catalog seed and everything derived from it (rights-registry rows, IMDb ratings) -- the catalog ships with the schema, never as a separate manual step (owner rule 2026-09-05). Every step is idempotent: redeploy this service on every release, before `backend`. It needs no public domain and no restart-on-success.",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "apps/backend/Dockerfile"
  },
  "deploy": {
    "startCommand": "npm run release",
    "numReplicas": 1,
    "restartPolicyType": "NEVER"
  }
}
