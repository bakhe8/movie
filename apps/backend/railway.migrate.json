{
  "$schema": "https://railway.app/railway.schema.json",
  "//": "Board C-18 (ADR-87). A SEPARATE Railway service (not the backend service above), same repo/root directory/Dockerfile, but overriding the start command to run pending migrations once and exit -- mirrors docker-compose.prod.yml's `migrate` profile, since Railway has no native pre-deploy/release-phase hook to rely on without live testing. Trigger a redeploy of THIS service manually before redeploying `backend` whenever a commit adds a migration; it needs no public domain and no restart-on-success.",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "apps/backend/Dockerfile"
  },
  "deploy": {
    "startCommand": "npm run migrate",
    "numReplicas": 1,
    "restartPolicyType": "NEVER"
  }
}
