# POSTERS-MULTI P5 — اتجاهات عرض مجموعة البوسترات (2026-09-06)

مصدر لوحة Claude Design «مجموعة بوسترات الفيلم». الرأس منقول من `WorkScreen.module.css` بقيمه الفعلية (مظهر cinema، الثيم الداكن).

- `DirectionA` النقاط · `Main` اتجاه ب المصغّرات (تفاعلي) · `DirectionC` الرزمة · `Cards` الأسطح التي لا تتغير · `Desktop` ب على ≥900px · `Story` storyboard ب · `Rules` عقد التفاعل.
- الصور غير مُلتزمة (بوسترات TMDB تُعرض بالرابط ولا تُخزَّن محليًا): تُجلب عند إعادة البناء إلى مجلد مؤقت بالمسارات في `title_posters` لـ`DEMO0137` (`inter-1..4.jpg`، مقاس w342) و`DEMO0043` (`cap-1..4.jpg`، مقاس w185).
- إعادة البناء: مولّد `gen-posters-canvas.py` (خارج المستودع) ثم أداة seed الخاصة بمهارة design مع `--image` لكل صورة.
