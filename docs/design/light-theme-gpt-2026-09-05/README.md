# L0 — مرجع طاولة المونتاج وخريطة الدلالات

الأساس: `30048ff0f4667b451940ea80019880f25d35b9df`؛ العقد: ADR-112 و`docs/THEME_MODES_2026-09-05.md`.
اعتمد المالك النموذج التفاعلي المكتمل ثم أجاز الانتقال إلى المنتج في 2026-09-05. هذه موافقة تصميم وتنفيذ؛ ليست قبول L3 ولا إذن نشر الإنتاج.

## الأصل المعتمد

`approved-reference.png` نسخة مطابقة بايتًا للصورة المختارة، 1672×941؛ لم تُعد توليدها أو تعديلها.
SHA-256: `bd81dc1ff31fdde1113a874e79f14956e313702bf0c179e50da306798c0d0802`.
الألوان أدناه مأخوذة من النموذج المعتمد وتدقيق التباين، وليست ادعاء استخراج بكسلات الصورة. شريط الفيلم معالجة بصرية؛ لا يحل محل علامة Kolme ذات البطاقات الثلاث المعتمدة في ADR-111.

## خريطة الرموز المجمدة قبل L1

| الرمز | الفاتح | الدلالة |
|---|---|---|
| `--bg` | `#f7f6fc` | أرضية فاتحة هادئة |
| `--surface` | `#ffffff` | السطح الأمامي |
| `--well` | `#eceeff` | تجميع المحتوى وغسلة ثانوية |
| `--line` | `#7b83a8` | حدود عناصر التحكم المرئية؛ ≥3:1 على الأرضيات |
| `--text` | `#202033` | النص الأساسي |
| `--muted` | `#57596e` | النص الثانوي، وليس نصًا غير قابل للقراءة |
| `--accent` / `--accent-ink` | `#2445e8` / `#ffffff` | الفعل الأساسي والعلامة؛ الأزرق يظل لون الإجراء |
| `--accent-soft` | `#eceeff` | اختيار ثانوي وإبراز هادئ |
| `--role-safe` / `--role-discovery` / `--role-outside` | `#2445e8` / `#334a86` / `#57596e` | تسميات المسارات الثلاثة؛ تبقى الأسماء ظاهرة ولا يحمل اللون المعنى وحده |
| `--role-later` | `#334a86` | الحفظ لوقت لاحق؛ ليس تفضيلًا |
| غسلات الأدوار الأربعة | `#eceeff` | أرضيات مساندة لا أزرار إضافية |
| `--position-highlight` / `--position-highlight-ink` | `#e9ff72` / `#202033` | علامة صغيرة لموضع الترتيب أو المسار النشط فقط؛ لا جودة ولا ملاءمة ولا ثقة ولا نجاح عام |
| `--poster-edge` | `var(--line)` | حد الملصق |
| `--shadow-float` | `0 12px 32px rgba(36,69,232,.14)` | الطبقات العائمة فقط |

الليموني ليس لون كل زر أو كل بطاقة. يتطلب نصًا داكنًا وحدًا أزرق إذا كان هو المؤشر الوحيد للاختيار؛ تباينه وحده مع الأبيض لا يكفي. رمزا الموضع يضافان في L1 للاستهلاك المحدود في L2، ولا يغيران الشاشة قبل ذلك. في الداكن يرثان الغسلة النيلية ونص الفعل الموجودين؛ كل قيم الداكن السابقة تبقى كما هي.

## مراجعة التباين قبل التنفيذ

النسب أدناه مرتبة على `bg / surface / well` وفق WCAG sRGB؛ حد النص 4.5:1 وحد العناصر غير النصية 3:1.

| الزوج | النسب |
|---|---|
| النص الأساسي | 14.85 / 15.96 / 13.86 |
| النص الثانوي والخارج عن المعتاد | 6.39 / 6.86 / 5.96 |
| الأزرق | 6.32 / 6.79 / 5.90 |
| الاكتشاف والحفظ | 7.93 / 8.52 / 7.40 |
| الحد | 3.46 / 3.71 / 3.23 |
| أبيض على زر أزرق | 6.79 |
| نص داكن على الليموني | 14.45 |

## تسلسل التسليم

| الحزمة | الحالة |
|---|---|
| L0 | الأصل محفوظ؛ الدلالات مثبتة من النموذج المعتمد؛ فحص التباين ناجح |
| L1 | تلي هذا الالتزام؛ رموز الفاتح وإقلاعه وأيقونته واختبارات عدم الانحدار فقط |
| L2 | تبدأ بعد دمج L1، باستخدام الشاشات والمنطق الحاليين؛ لا نقل بيانات النموذج التجريبية إلى المنتج |
| L3 | قبول مستقل ومصفوفة لقطات كاملة مطلوبة؛ لا تعني اختبارات L1 أن الشاشات مكتملة |
