# خريطة مذكرة الإصلاح (KOLME_REEL_IMPLEMENTATION_REMEDIATION_BRIEF_AR v1.0) مقابل الشيفرة

مثبَّتة على commit `fe36ab6` (`main`، 2026-09-05). المذكرة اختبرت **النسخة الحيّة** black-box؛ هذه الخريطة تجيب سؤالاً واحداً لكل تذكرة: هل السبب في الشيفرة الحالية، أم في نشر الإنتاج، أم قرار منتج لم يُتخذ؟

## 0. وسوم الأدلة

| الوسم | المعنى |
|---|---|
| **شيفرة** | مقروء في المستودع عند `fe36ab6`؛ يمكن إعادة إنتاجه محلياً |
| **إنتاج** | لا يُحسم إلا من Railway (متغيّرات/سجلات/قاعدة الإنتاج)؛ ليس لديّ وصول |
| **قرار** | يحتاج قرار المالك قبل أي كود |

## 1. التذاكر

| التذكرة | ما هو موجود فعلاً (شيفرة) | ما تثبته المذكرة ولا تفسّره الشيفرة | السبب المرجّح | الإجراء |
|---|---|---|---|---|
| `TRAIN-01` P0-01 | مشغّل تلقائي عند 3 جولات ثم كل 5 (`TrainingService`، subscriber بعد commit، ADR-87) · `POST /train` 202 · `GET /training` بثماني حالات (`disabled/paused/idle/queued/running/succeeded/failed/unknown`) · خدمة النموذج HTTP (ADR-25/74) | زر «حدّث نموذجي» بلا أي ردّ · 10 جولات بلا نموذج | **شيفرة**: `ProfileScreen.retrain()` يبتلع كل خطأ (`catch {}`)؛ الشاشة تعرض `failed`/`disabled`/`idle` كلها كـ«لم يُدرَّب بعد» بلا سبب · `GET /recommendations` يعيد `pending` بـ`needed` فقط، وهو 0 بعد الجولة الثالثة → «ما زلنا نتعلم» إلى الأبد إن كان التدريب معطَّلاً أو فاشلاً · لا CTA للتدريب في الرئيسية · **إنتاج**: هل `MODEL_SERVICE_URL` مضبوط على خدمة الخلفية وخدمة `model-service` منشورة؟ (السطر `MODEL_SERVICE_URL is not set` في سجل الخلفية يحسمها) | (أ) إظهار حالة التدريب وسببها في الرئيسية والملف، وخطأ `/train` نصاً · (ب) `pending` يحمل حالة التدريب لا `needed` فقط · (ج) فحص الإنتاج §3 |
| `CAT-01` P0-02 | 300 عنواناً في `catalog.demo.json`: 300 بصمة كاملة، 300 وصف عربي، 297 ملصق (الناقص: Sattar · Naga · The Wedding of Zein)، **0 أجناس عربية** · الإصدار = هجرات + `seed --catalog-only` + حقوق + IMDb في عملية واحدة تفشل بصوت (ADR-90) · 125 صفاً إضافياً مُدرَجة في `catalog.demo.list.tsv` (غير ملتزَمة، جلسة الكتالوج) | «الكتالوج كاملاً: 12» · 7 أفلام «لم تُنشر بصمتها» | **إنتاج**: 12 ليست 15 القديمة ولا 300 → إصدار ADR-90 لم يكن قد جرى وقت الاختبار أو جرى جزئياً؛ المدرِّب يرفض الثلاثيات التي بصماتها ناقصة (`training.py:408`) فيفشل التدريب `invalid` — هذا يفسّر «لا نموذج» في الاختبار بالكامل إن صحّ | التحقق بعد النشر الحالي: `[release] catalog: N titles` في سجل النشر و`/api/admin/metrics → catalog` · health يفشل إن كان الكتالوج 0 (§2 بند 1) |
| `OBS-01` P0-03 | `/api/health` 200 حيّ · Sentry/OTel خلف أعلام (ADR-86) · الحالات المصمَّمة 200 + discriminator (ADR-81) · `DiscoverScreen` يفرّق `loadFailed` (alert) عن `noResults` | الكتالوج الفارغ ظهر «لا نتائج» | **شيفرة جزئياً**: قاعدة فارغة تعيد 200 وقائمة فارغة → «لا نتائج» صادقة تقنياً لكنها تخفي عطل نشر · لا `x-request-id` في الخلفية · لا فحص عدّ الكتالوج في health | health يعيد 503 إن كان `titles = 0` · معرّف طلب يُمرَّر من الواجهة إلى السجلات ويُعرض مع أي فشل |
| `TRIAD-01` P0-04 | `displayOrder` مخلوط ومحفوظ ✓ · `policyVersion` ✓ · `selectionPropensity` ✓ · `holdout` (false دوماً) ✓ · `idempotency-key` ✓ · الترتيب listwise مصفوفة واحدة ✓ · «لم أشاهده»/«لا أتذكره» محايدان (ADR-17) ✓ | تكرار مجموعة الجولة 2 في الجولة 4 واحتسابها | **شيفرة، بقرار سابق**: `random-v1` يستثني عناوين **الجولة السابقة فقط** (ADR-34)؛ مع 8 أفلام يبقى 5 → 10 مجموعات ممكنة، فتكرار مجموعة أقدم p≈1/10 لكل جولة، بلا وسم، ويدخل `completedTriads` | ADR يعدّل ADR-34: عمود `setHash` + `purpose` (`learn`/`verify`) + `countTowardActivation`؛ الاستثناء يشمل كل المجموعات المكتملة ما دام البديل متاحاً، وبعدها تُوسم `verify`؛ عتبة التدريب والعدّاد بالمجموعات الفريدة |
| `REC-01` P0-05 | ثلاثة مسارات بحصة استكشاف (ADR-84) · Public Quality منفصلة بمصدر وأصوات (ADR-77/78) · نطاقات ثقة بأرضية دقة محجوبة (ADR-79) · تفسير قالبي حتمي من الأدلة (§9.4، لا LLM في المسار الحيّ) · حلقة ما بعد المشاهدة حتى `ranked_later` (ADR-66–68) · ترتيب المكتبة positions-only (`/library/ranking`) | لم يُختبر لأن النموذج لم يُنشر | يتبع `TRAIN-01` | بعد `TRAIN-01`: اختبار §7.2 (ملفان متعاكسان) على dev — الشخصيات التجريبية موجودة (`personas.demo.json`) |
| `PRIV-01` P0-06 | `personalization_pooled` يبدأ **مفعَّلاً** عمداً (PRIVACY.md §3) · `analytics_first_party` يبدأ **معطَّلاً** · لا حدث تحليلات قبل الموافقة (ADR-86) · اليوميات لا يقرأها المدرِّب (لا `notes` في `training.py`) · تصدير/حذف/إعادة ضبط موجودة مع e2e (ADR-73/80) | «كلتاهما مفعَّلتان افتراضياً» | **قرار** + **إنتاج**: الشيفرة تخالف الملاحظة في التحليلات (بناء أقدم أو قراءة خاطئة — يُعاد الفحص)؛ أما المجمَّع فمفعَّل بقرار موثَّق والمذكرة تطلب عكسه | **O-6**: هل يصبح `personalization_pooled` opt-in؟ ثم سطر واحد في `OnboardingScreen` + PRIVACY.md §3 |
| `LIC-01` P0-07 | سجل `catalog_rights` لكل حقل · `DATA_LICENSING.md` · IMDb غير تجاري (ADR-77) · الاتفاقيات مؤجَّلة إلى ما بعد الإطلاق المجاني (ADR-72، قرار المالك) · الملصقات تُعرض بلا بوابة ترخيص (`d556655`، قرار المالك) | — | متسق مع المذكرة («قبل أي إطلاق **تجاري**») | لا عمل الآن؛ يبقى `license_status` مملوءاً لكل صف جديد |
| `SEARCH-01` P1-01 | `ILIKE` على `titleEn` و`titleAr` + طيّ عربي (همزة/تاء مربوطة) · لا `localized_titles` ولا بدائل (فجوة M3 معروفة) | `Anora` صفر و«أنورا» واحد | **إنتاج**: `titleEn` في الفكستشر = `Anora` و`ILIKE` لا يميّز الحالة → الشيفرة الحالية لا تنتج هذه النتيجة؛ صف الإنتاج وقتها كان من مصدر آخر | اختبار e2e بمجموعة ذهبية (50 عنواناً ar/en/حالة أحرف) · `unaccent` للاتينية · بحث في `localized_titles` |
| `ACT-01` P1-02 | العتبات إعدادات (3، ثم +5) وتُعرض في `GET /training` (`nextTrainingAt`) · نصوص الواجهة تثبّت «ثلاث إلى خمس جولات» و«ثلاثة أفلام» | الوعد لا يطابق الرياضيات | **شيفرة**: `RankScreen` لا يقرأ `nextTrainingAt` | `RankScreen` يعرض «جولة n من N» من الخلفية · بعد أول ثلاثية: دفع إلى 6–10 أفلام (نص، قرار محرّر) |
| `DATE-01` P1-03 | `watchedAt` طابع لحظي `timestamp` لا `DATE` · `ListScreen` يشتق تاريخ اليومية من `watchedAt.slice(0,10)` (يوم UTC لا المحلي) و**يعيد إرسال `watchedAt` مع كل حفظ يومية** (`ListScreen.tsx:207,223`) | فرق يوم · تغيّر التاريخ بعد حفظ اليومية | **شيفرة**: مثبت — حفظ اليومية يكتب التاريخ من جديد ولو لم يلمسه المستخدم | عمود `watchedOn DATE` (هجرة تُعبّئه من `watchedAt`) · حفظ اليومية يرسل `notes` فقط ما لم يُعدَّل التاريخ · اختبار حدود منتصف الليل |
| `AVL-01` P1-04 | لا `market`/`platforms` في `recommendations.service.ts` إطلاقاً → الفصل قائم بالبناء · `availability_snapshots` كيان بلا مصدر بيانات → التوفر `unknown` للجميع | غير مختبر | **شيفرة** (سليم)، ينقصه إثبات | اختبار عقد: تغيير السوق لا يغيّر `fit` · حقل `availability: unknown` صريح في عنصر التوصية |
| `L10N-01` P1-05 | الأجناس نصوص إنجليزية تُعرض كما هي (`WorkScreen`) · 0/300 عربية · `PublicQualityCell` يعرض المصدر والأصوات **ولا يعرض `capturedAt`** رغم وجوده في النوع · `Poster` يعيد `null` بلا عنصر نائب مصمَّم | مثبت | **شيفرة**: مثبت كله | قاموس أجناس ar/en في الواجهة (مفردات منضبطة) · «بتاريخ …» من `capturedAt` · عنصر نائب للملصق |
| `BRAND-01` P1-06 | `Reel` في `layout.tsx`/`manifest.ts`/`AppShell`/`AuthScreen`/نصوص الشروط والخصوصية؛ النطاق `kolme.app` | مثبت | **قرار** | **O-7**: اسم واحد أم «Reel من Kolme»؟ ثم استبدال في 6 ملفات |
| `TRY-01` P2 | — | — | بعد القلب، كما تقول المذكرة | لا شيء الآن |

مغلق من الجولة الحيّة نفسها: طيّ حالة أحرف البريد (`fe36ab6`)، استعادة كلمة المرور من الواجهة (`bae231c`).

## 2. الطابور الناتج (بالترتيب)

| # | العمل | التذاكر | حجمه |
|---|---|---|---|
| 1 | إنهاء الفشل الصامت: `pending` يحمل `training: { state, errorKind, jobId }`؛ الرئيسية والملف يعرضان السبب وCTA «درّب الآن»؛ خطأ `/train` يظهر نصاً؛ health 503 عند كتالوج 0 | TRAIN-01 · OBS-01 | صغير، لا هجرة |
| 2 | فحص الإنتاج (§3) وإعادة تشغيل الجولة الذهبية §7.1 بحساب المالك | TRAIN-01 · CAT-01 | صفر كود |
| 3 | ADR يعدّل ADR-34: `setHash` + `purpose` + `countTowardActivation`؛ العتبة بالمجموعات الفريدة | TRIAD-01 · ACT-01 | متوسط، هجرة واحدة |
| 4 | `watchedOn DATE` + حفظ اليومية لا يمسّ التاريخ | DATE-01 | صغير، هجرة واحدة |
| 5 | أجناس عربية + `capturedAt` + عنصر نائب للملصق | L10N-01 | صغير |
| 6 | مجموعة البحث الذهبية + `unaccent` + `localized_titles` | SEARCH-01 | متوسط |
| 7 | اختبار عقد الفصل + `availability: unknown` صريح | AVL-01 | صغير |
| 8 | حالات الجاهزية الأربع (§5.1 في المذكرة) كعقد API واحد تقرأه الواجهة | TRAIN-01 · REC-01 | ADR + متوسط؛ بعد 1 و3 |

## 3. ما يحسمه المالك وحده (Railway)

1. خدمة الخلفية: `MODEL_SERVICE_URL` و`MODEL_SERVICE_TOKEN` مضبوطان؟ وهل يظهر في سجلها `MODEL_SERVICE_URL is not set`؟
2. خدمة `model-service` منشورة و`/health` فيها يجيب؟
3. آخر نشر: سطر `[release] catalog: N titles` — N = 300؟
4. `MAIL_TRANSPORT=resend` و`MAIL_FROM_ADDRESS` (معلّق منذ الجولة السابقة).
5. القراران **O-6** (المجمَّع opt-in؟) و**O-7** (Kolme/Reel).
